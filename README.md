# hyd-f

Monorepo layout:

- `frontend/` — Next.js app (Hydrilla website + studio)
- `backend/` — API server (local / separate deploy)

## Develop

```bash
cd frontend
npm install
npm run dev
```

## Deploy (Vercel)

Set the project **Root Directory** to `frontend`.
